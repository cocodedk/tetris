## What and why

<!-- One or two sentences: what this changes and why. Link the issue if there is one. -->

## Checklist

- [ ] `node --test` passes
- [ ] New behaviour has a test (the state that drives the pixels, not the pixels)
- [ ] `src/core/` and `src/fx/` still touch no `window`, `document` or canvas
- [ ] Every code file stays under 200 lines
- [ ] Both pages (`index.html`, `fa/index.html`) still work, if the UI changed
